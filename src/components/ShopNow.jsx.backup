import React, { useState } from 'react'
import { ShoppingCart, Package, AlertCircle, ArrowLeft } from 'lucide-react'

const ShopNow = ({ onBack }) => {
  const [formData, setFormData] = useState({
    name: '',
    address: '',
    city: '',
    state: '',
    pincode: '',
    landmark: '',
    contact1: '',
    color: ''
  })

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    })
  }

  const handleSubmit = (e) => {
    e.preventDefault()
    
    // Create WhatsApp message
    const message = `*New PixelOne Order* 🎮\n\n` +
      `*Customer Details:*\n` +
      `Name: ${formData.name}\n` +
      `Contact 1: ${formData.contact1}\n` +
      `${formData.contact2 ? `Contact 2: ${formData.contact2}\n` : ''}` +
      `\n*Shipping Address:*\n` +
      `${formData.address}\n` +
      `City: ${formData.city}\n` +
      `State: ${formData.state}\n` +
      `Pincode: ${formData.pincode}\n` +
      `\n*Product Details:*\n` +
      `Color: ${formData.color}\n` +
      `*Total Amount: ₹3,756*\n` +
      `(Free Shipping India Wide)`

    // Replace with your WhatsApp number
    const phoneNumber = '919880700926' // Format: country code + number (no + or spaces)
    const whatsappUrl = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(message)}`
    
    window.open(whatsappUrl, '_blank')
  }

  const colors = [
    { id: 'coral', name: 'Coral Pink', color: '#FF9B8A' },
    { id: 'teal', name: 'Teal Blue', color: '#4A7C7E' },
    { id: 'navy', name: 'Navy Blue', color: '#2C3E50' },
    { id: 'beige', name: 'Beige', color: '#E8E0D5' }
  ]

  const sizes = ['Standard', 'Compact', 'Pro']

  return (
    <section className="min-h-screen pt-24 pb-12 bg-gradient-to-b from-pixel-bg to-white">
      <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">

        {/* Header */}
        <div className="text-center mb-12">
          <div className="inline-block bg-pixel-coral/10 rounded-full px-6 py-2 mb-4">
            <span className="font-pixel text-xs text-pixel-coral">CHECKOUT</span>
          </div>
          <h1 className="font-pixel text-3xl sm:text-4xl lg:text-5xl text-pixel-dark mb-4">
            COMPLETE YOUR <span className="text-pixel-coral">ORDER</span>
          </h1>
          <p className="text-pixel-dark/70 max-w-2xl mx-auto">
            Fill in your details below to place your order. We'll contact you for payment confirmation.
          </p>
        </div>

        {/* Order Form */}
        <div className="bg-white rounded-3xl shadow-2xl p-8 lg:p-12 border-2 border-pixel-dark/5">
          <form onSubmit={handleSubmit} className="space-y-6">
            {/* Form fields will continue here */}
            
            {/* Important Note */}
            <div className="bg-gradient-to-r from-pixel-coral/10 to-pixel-teal/10 rounded-2xl p-6 border-2 border-pixel-coral/20">
              <div className="flex items-start gap-3">
                <AlertCircle className="text-pixel-coral flex-shrink-0 mt-1" size={24} />
                <div>
                  <h4 className="font-bold text-pixel-dark mb-2">Important Note:</h4>
                  <p className="text-sm text-pixel-dark/80 leading-relaxed mb-2">
                    🎦 <strong>360° Unboxing Video Of Parcel</strong> (from the beginning to end) is <strong>mandatory</strong> for exchange of wrong or defective product received.
                  </p>
                  <p className="text-sm text-pixel-coral font-semibold">
                    Otherwise no complaint will be attended.
                  </p>
                </div>
              </div>
            </div>

            {/* Order Summary */}
            <div className="bg-pixel-bg rounded-2xl p-6">
              <h4 className="font-bold text-pixel-dark mb-4">Order Summary</h4>
              <div className="space-y-2 text-sm">
                <div className="flex justify-between">
                  <span className="text-pixel-dark/70">Product Price</span>
                  <span className="font-semibold text-pixel-dark">₹3,756</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-pixel-dark/70">Shipping</span>
                  <span className="font-semibold text-pixel-teal">FREE</span>
                </div>
                <div className="border-t border-pixel-dark/10 pt-2 mt-2">
                  <div className="flex justify-between text-lg">
                    <span className="font-bold text-pixel-dark">Total Amount</span>
                    <span className="font-bold text-pixel-coral">₹3,756</span>
                  </div>
                </div>
              </div>
            </div>

            {/* Submit Button */}
            <button
              type="submit"
              className="w-full py-4 bg-gradient-to-r from-pixel-coral to-pixel-salmon text-white font-bold rounded-xl hover:scale-105 transition-all duration-300 shadow-xl flex items-center justify-center gap-2 group"
            >
              <ShoppingCart size={20} className="group-hover:animate-bounce" />
              Order Now - ₹3,756
            </button>

            <p className="text-xs text-center text-pixel-dark/50">
              Your order will be sent via WhatsApp for confirmation • Free shipping India wide • 2-7 days delivery
            </p>
          </form>
        </div>
      </div>
    </section>
  )
}

export default ShopNow
